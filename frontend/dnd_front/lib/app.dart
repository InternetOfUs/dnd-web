import 'package:flutter/material.dart';

import 'dnd/dnd.dart';

class DnDApp extends MaterialApp {
  /// {@macro counter_app}
  const DnDApp({Key? key}) : super(key: key, home: const LoginPage());
}
